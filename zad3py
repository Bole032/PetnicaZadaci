import pandas as p
t=10
df = p.read_csv("files/order_products__prior.csv")
nar = len(set(df["order_id"]))
proiz = len(set(df["product_id"]))

niz = [0] * 3 * t

for i in df["product_id"]:
    niz[i] += 1
niz.sort()

x = niz[-t:]

for i in x:
print(i)
